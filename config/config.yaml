gradio:
  concurrent: 20
openai:
  api_url: "https://api.openai.com/v1/chat/completions"
  content_token: 3000 # tokens per content_main (e.g. transcript). If exceed it will be splitted and iterated
  timeout_sec: 25
  max_retry: 2
  api_key: ""